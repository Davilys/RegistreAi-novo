export const WINDOW=86400000;export function decideOutboundPolicy(i:{now:Date;lastInboundAt?:Date;templateName?:string;optedOut?:boolean;handoffActive?:boolean}){if(i.optedOut)return{allowed:false,reason:"CUSTOMER_OPTED_OUT"};if(i.handoffActive)return{allowed:false,reason:"HUMAN_HANDOFF_ACTIVE"};if(i.lastInboundAt&&i.now.getTime()-i.lastInboundAt.getTime()<=WINDOW)return{allowed:true,mode:"session"};if(i.templateName)return{allowed:true,mode:"template"};return{allowed:false,reason:"TEMPLATE_REQUIRED_OUTSIDE_24H"}}export function classifyControlIntent(t:string){return/^(parar|sair|stop)$/iu.test(t.trim())?"opt_out":/^(falar com humano|atendente|especialista)$/iu.test(t.trim())?"handoff":null}export function retryDisposition(h:number,c?:number){if(h===401||h===403||[190,200,10].includes(c??-1))return"reauth";if([400,404].includes(h)||[131026,131047,131050].includes(c??-1))return"do_not_retry";return h===429||h>=500?"retry":"do_not_retry"}
