# Variáveis públicas usadas no build do site
VITE_SUPABASE_URL=https://qxgfkpqlaffsesqtnojh.supabase.co
VITE_WHATSAPP_NUMBER=

# Hash bcrypt da senha do /admin (usuário davilys). Nunca versionar o valor real.
# Gerar no VPS: docker compose exec caddy caddy hash-password
# Use aspas simples para o Docker Compose não interpretar os "$" do hash.
ADMIN_BASIC_AUTH_HASH='$2a$14$troque-pelo-hash-gerado'
